#include "main.h"

/**
*_abs - prints the sign of the number
*@n: The number that will be checked
*
*Return: absolute value of n
*
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
